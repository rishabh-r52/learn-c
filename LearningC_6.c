#include <stdio.h>
int main(void)
{
    int no_of_teams,no_of_sockets,power_strips;
    printf("Enter the no. of teams:");
    scanf("%d",&no_of_teams)
    printf("\nEnter the no. of power sockets in one power strip:");
    scanf("%d",&no_of_sockets)
    if (no_of_teams <= no_of_sockets)
        power_strips = 1;
    else
    {
        power_strips = 
    }
}